server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Static files
  location /assets/ {
    try_files $uri =404;
  }

  # SPA fallback for all client-side routes
  location / {
    try_files $uri /index.html;
  }
}
